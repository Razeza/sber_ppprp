apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-cm
  namespace: default
data:
  default.conf: |-
    server {
        listen 80;
        location /pokemons/first {
            proxy_pass http://pokemon-service-cip/pokemons/get-first;
        }
    }
